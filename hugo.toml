baseURL = 'https://sandray-5.github.io/'
languageCode = 'en-us'
title = "Sandra's Galaxy"
theme = "PaperMod"

# 配置
[params]
  # 1. Basic setting
  env = "production"
  # 避免与extend_head.html冲突
  math = false
  title = "Sandra's Galaxy"
  description = "Statistics & Life"
  # author = "Sandra"

  # 2. Page Function
  ShowToc = true          # 显示目录
  TocOpen = true          # 默认展开目录
  ShowCodeCopyButtons = true # 显示代码复制按钮 
  dateFormat = "2006-01-02" # 设置日期格式
  ShowReadingTime = true    # 显示阅读时间
  ShowWordCount = true      # 显示字数

  # 3. Sections
  mainSections = ["tech", "life"] # 对应content下面两个文件夹

  # 4. favicon
  [params.assets]
    favicon = "favicon.png"
    favicon16x16 = "favicon.png"
    favicon32x32 = "favicon.png"
    apple_touch_icon = "favicon.png"

  # 5. comment
  [params.comments]
    provider = "giscus"
  
  # 6. Giscus setting
  [params.giscus]
    repo = "SandraY-5/SandraY-5.github.io"
    repoID = "R_kgDOQlM_dw"
    category = "General"
    categoryID = "DIC_kwDOQlM_d84Czksp"
    mapping = "url"
    strict = "0"
    reactionsEnabled = "1"
    emitMetadata = "0"
    inputPosition = "bottom"
    theme = "light"
    lang = "zh-CN"

  # 7. bio
  [params.homeInfoParams]
    Title = "Hi there 👋"
    Content = "Hi, I'm Sandra. Sharing bits of tech and life. Happy you're here."

  # 8. social Icons
  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/SandraY-5"
  [[params.socialIcons]]
    name = "email"
    url = "https://sandray-5.github.io/about/"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    noClasses = false
  # 关键修复：确保 Goldmark 不会转义 $ 符号
  [markup.goldmark.extensions]
    [markup.goldmark.extensions.passthrough]
      enable = true
      [markup.goldmark.extensions.passthrough.delimiters]
        block = [['\[', '\]'], ['$$', '$$']]
        inline = [['\(', '\)'], ['$', '$']]
# --- End of [params] ---


# --- 导航栏配置 (Menu) ---
[menu]
  [[menu.main]]
    identifier = "tech"
    name = "Lab"
    url = "/tech/"
    weight = 10
  [[menu.main]]
    identifier = "life"
    name = "Life"
    url = "/life/"
    weight = 20
  [[menu.main]]
    identifier = "tags"
    name = "Tags"
    url = "/tags/"
    weight = 30
  [[menu.main]]
    identifier = "search"
    name = "Search"
    url = "/search/"
    weight = 40
  [[menu.main]]
    identifier = "archives"
    name = "Archive"  # 或者叫 "Archive"
    url = "/archives/"
    weight = 50        # 排在 Search 后面


[outputs]
  home = ["HTML", "RSS", "JSON"] # 必须加上 JSON，搜索功能依赖它